name: clean_arch_resocoder
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.5 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  bloc: ^8.1.0
  equatable: ^2.0.5
  get_it: ^7.2.0
  dartz: ^0.10.1
  connectivity: ^3.0.6
  http: ^0.13.5
  shared_preferences: ^2.0.17

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  mockito: ^5.3.2

flutter:

  uses-material-design: true